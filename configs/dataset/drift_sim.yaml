_target_: src.tracknet.data.dataset.PrebatchedStrawTracksDataset

cache_dir: ${user_settings.project_path}/outputs/drift_sim_cache
batch_size: ${training.batch_size}
num_tubes: 1208
split_seed: ${training.seed}
shuffle_shards: true
input_columns: [x0, y0, z0, dr, station]
station_tube_counts: [151, 151, 151, 151, 151, 151, 151, 151]
